# Satori Lite Requirements
# Minimal dependencies for lightweight Satori neuron

# Core dependencies
setuptools>=78.1.1

# Data handling - pinned for compatibility
# numpy floor is 1.26.4: required by timesfm 2.x and still numpy 1.x, so
# pandas 1.5.3 (which breaks on numpy 2.x) stays compatible.
numpy==1.26.4
pandas==1.5.3
pyarrow>=14.0.1
fastparquet==2022.12.0

# Configuration
PyYAML>=6.0

# System utilities
psutil>=5.9.0
joblib==1.2.0

# AI Engine - Machine Learning
xgboost==1.7.2
scikit-learn>=1.5.0
lightgbm>=4.5.0
catboost>=1.2.7
statsmodels>=0.14.4
skforecast>=0.13.0
pmdarima>=2.0.4
linear-tree>=0.3.5
sktime>=0.33.1
optuna>=4.0.0

# AI Engine - Deep Learning (optional, for advanced models)
# torch (CPU wheel) + timesfm==2.0.1 are installed in the Dockerfile in a
# dedicated step AFTER this file, so timesfm does not drag in the ~2 GB CUDA
# torch. They power the optional TimesFmAdapter (auto-hidden if unavailable).
transformers>=4.48.0
# prophet>=1.1.6  # has complex dependencies
# tbats>=1.1.3
# neuralforecast>=1.7.5

# Lomb-scargle interpolation
astropy>=6.1.7

# Wallet dependencies - Ravencoin and Evrmore blockchain
python-ravencoinlib
python-evrmorelib
mnemonic>=0.20
base58>=2.1.0

# Encryption for wallet vault
pycryptodome>=3.20.0

# Ethereum wallet support
eth-account>=0.13.0
eth-keys>=0.6.0
web3>=7.0.0
coincurve>=20.0.0

# API validation
marshmallow>=3.22.0

# Networking
requests>=2.32.0
websockets>=15.0.0
# 0.45 is a FLOOR, not a preference: satorilib's nostr client was migrated to
# the 0.45 API on 2026-08-18 and no longer runs on 0.44.x. Neuron reaches Nostr
# through satorilib rather than directly, so it has no call sites of its own to
# change, but it must not resolve the SDK below what satorilib needs.
nostr-sdk>=0.45.0
docker>=7.1.0

# Web UI
flask>=3.0.0
flask-cors>=4.0.0
qrcode[pil]>=7.4.0

# Data encryption
cryptography>=44.0.0

# IP address validation
ipaddress>=1.0.0

# Centrifugo real-time messaging
centrifuge-python>=0.4.1

# Optional: Telemetry logging
# Uncomment lines below to enable telemetry
# sanic>=23.6.0
# aiosqlite>=0.19.0
# uvloop>=0.19.0
